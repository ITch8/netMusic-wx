<swiper indicator-dots="false" autoplay="true" interval="3000">
  <block wx:for="{{swiperList}}" wx:key="{{key}}">
    <swiper-item>
      <image src='{{item.url}}' mode='widthFix'></image>
    </swiper-item>
  </block>
</swiper>
<play-list list="{{playlist}}" title="为你推荐"/>
<play-list list="{{officialPlayList}}" title="官方歌单"/>